# Training outputs (gitignored)

Local fine-tuning run outputs may be stored here. Weight files and checkpoints
are gitignored; this `.gitkeep` keeps the directory visible in the repo layout.

See [`../../README.md`](../../README.md) for the fine-tuning methodology.
